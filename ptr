#include <iostream>
using namespace std;

int main() {
    int x = 10;
    int* ptr = &x;

    cout << "x 的值: " << x << endl;
    cout << "x 的地址: " << &x << endl;
    cout << "指针 ptr 指向的值: " << *ptr << endl;

    return 0;
}
